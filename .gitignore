# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments. Match by shape, not by name — this file previously
# listed only LLM/ and a venv312/ on another machine was committed whole.
LLM/
venv*/
.venv/
env/
ENV/
pyvenv.cfg

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Model checkpoints (too large for git). Adapters travel by GitHub release,
# not by pull — see STATUS.md.
runs/*/checkpoint/
models/
*.bin
*.safetensors
*.pt
*.ckpt
*.pth

# Release archives. checkpoints.tar.gz is 99MB and already published.
*.tar.gz
*.tgz
*.zip

# HuggingFace cache
.cache/

# Environment
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Internal working notes — kept locally, not published with the code.
STATUS.md
ASSISTANT_PROMPT.md
